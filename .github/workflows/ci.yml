name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build

  e2e:
    runs-on: ubuntu-latest
    # Im offiziellen Playwright-Container laufen (Browser + System-Fonts sind
    # fest eingebacken). Das macht die visuellen Regressionstests deterministisch:
    # Referenzbilder werden im *gleichen* Image erzeugt (siehe README).
    # Version muss zu @playwright/test in package.json passen.
    container:
      image: mcr.microsoft.com/playwright:v1.56.1-noble
    steps:
      - uses: actions/checkout@v7
      # Node + npm sind im Image gebündelt; Browser liegen unter
      # PLAYWRIGHT_BROWSERS_PATH. Nach einem Dependabot-Bump von
      # `@playwright/test` kann die im Image vorinstallierte Browser-Revision
      # veralten (Image-Tag bleibt bis zum manuellen Review fest) — deshalb
      # hier explizit die zur installierten Version passende Chromium-Revision
      # nachziehen (kein `--with-deps`: OS-Pakete sind im Image bereits da).
      - run: npm ci
      - run: npx playwright install chromium
      - run: npm run build
      - run: npm run test:e2e
      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
      # Bei visuellen Abweichungen legt Playwright expected/actual/diff-PNGs
      # unter test-results/ ab — zum Anschauen im Run.
      - uses: actions/upload-artifact@v7
        if: ${{ failure() }}
        with:
          name: playwright-diffs
          path: test-results/
          retention-days: 7
